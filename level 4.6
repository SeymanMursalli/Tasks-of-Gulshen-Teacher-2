#include <iostream>
using namespace std;
int main(){
    int n,x,m,b;
    double c=0;
    cin >> n >> x;
    m=b=x;
    c+=x;
    for(int i=1;i<n;i++){
        cin >> x;
        if(x>m) m=x;
        if(x<b) b=x;
        c+=x;
    }
    cout << m << " " << b << " " << c/n;
}